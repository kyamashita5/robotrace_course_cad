# Robotrace Course CAD

仕様書 `doc/robotrace_course_cad_spec.md` に基づく、補助円列編集と清書中心線描画の最小実装です。

```bash
python -m robotrace_course_cad
```

または、editable install 後:

```bash
pip install -e .
robotrace-course-cad
```

現時点では板領域描画、マーカー、交差点チェック、SVG/DXF/PDF出力は未実装です。
